#!/bin/bash
#
# emoji - emoji picker using rofi

emojis=~/.local/share/emojilib/emojis.json

jq -r 'to_entries[] | .value.char + " " + .key' "$emojis" |
rofi -dmenu -p emoji -matching normal -i -font 'serif 14' -lines 10 | {
    read -r emoji _
    clip -n "$emoji"
}
